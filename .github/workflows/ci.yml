name: CI

on:
  pull_request:
    branches: [development, main]
  push:
    branches: [development, main]

jobs:
  lint-and-test:
    name: Lint and test
    runs-on: ubuntu-latest

    services:
      postgres:
        image: postgres:16-alpine
        env:
          POSTGRES_DB: auction_test
          POSTGRES_USER: auction_user
          POSTGRES_PASSWORD: testpassword
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

      redis:
        image: redis:7-alpine
        ports:
          - 6379:6379
        options: >-
          --health-cmd "redis-cli ping"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: "pip"

      - name: Install dependencies
        run: |
          pip install -r backend/requirements.txt
          pip install -r backend/requirements-dev.txt

      - name: Run linter
        run: ruff check backend/

      - name: Run type checker
        run: mypy backend/app

      - name: Run tests (backend)
        env:
          DATABASE_URL: postgresql://auction_user:testpassword@localhost:5432/auction_test
          REDIS_URL: redis://localhost:6379/0
          APP_ENV: test
        run: cd backend && pytest tests/ -v --tb=short

      - name: Run tests (bidding-engine)
        run: |
          pip install -r bidding-engine/requirements.txt -r bidding-engine/requirements-dev.txt
          cd bidding-engine && pytest tests/ -v --tb=short

      - name: Run tests (recommendation-engine)
        run: |
          pip install -r recommendation-engine/requirements.txt -r recommendation-engine/requirements-dev.txt
          cd recommendation-engine && pytest tests/ -v --tb=short

  frontend-lint-and-build:
    name: Frontend lint and build
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"
          cache-dependency-path: frontend/package-lock.json

      - name: Install dependencies
        working-directory: frontend
        run: npm ci

      # Non-blocking: the codebase currently has ~63 pre-existing lint errors (mostly
      # no-explicit-any). Surface them without failing the build; flip to blocking
      # once that backlog is cleared.
      - name: Run linter
        working-directory: frontend
        run: npm run lint
        continue-on-error: true

      - name: Build
        working-directory: frontend
        run: npm run build

  integration-tests:
    name: Integration tests (real DB + Redis)
    runs-on: ubuntu-latest

    services:
      postgres:
        image: postgres:16-alpine
        env:
          POSTGRES_DB: auction_test
          POSTGRES_USER: auction_user
          POSTGRES_PASSWORD: testpassword
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
      redis:
        image: redis:7-alpine
        ports:
          - 6379:6379
        options: >-
          --health-cmd "redis-cli ping"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: "pip"

      - name: Install psql client
        run: sudo apt-get update && sudo apt-get install -y postgresql-client

      # init.sql is the base schema; scripts/apply_migrations.py then replays every file
      # under scripts/migrations/ (each one is self-guarded to be safely re-runnable) so
      # the drift tests check against the actual current schema, not a hand-picked subset.
      - name: Load schema
        env:
          PGPASSWORD: testpassword
          DATABASE_URL: postgresql://auction_user:testpassword@localhost:5432/auction_test
        run: |
          psql -h localhost -U auction_user -d auction_test -v ON_ERROR_STOP=1 -f docker/postgres/init.sql
          python3 scripts/apply_migrations.py

      - name: Install dependencies (all services)
        run: |
          pip install -r backend/requirements.txt -r backend/requirements-dev.txt
          pip install -r bidding-engine/requirements.txt -r bidding-engine/requirements-dev.txt
          pip install -r recommendation-engine/requirements.txt -r recommendation-engine/requirements-dev.txt

      - name: Integration — backend (schema drift)
        env:
          DATABASE_URL: postgresql+asyncpg://auction_user:testpassword@localhost:5432/auction_test
        run: cd backend && pytest integration/ -v --tb=short

      - name: Integration — recommendation-engine (schema drift)
        env:
          DATABASE_URL: postgresql+asyncpg://auction_user:testpassword@localhost:5432/auction_test
        run: cd recommendation-engine && pytest integration/ -v --tb=short

      # INTEGRATION_DATABASE_URL makes the harness skip local docker provisioning and use the
      # ephemeral CI database directly (it's isolated per run, so writes are fine).
      - name: Integration — bidding-engine (concurrency, escrow, drift)
        env:
          DATABASE_URL: postgresql+asyncpg://auction_user:testpassword@localhost:5432/auction_test
          INTEGRATION_DATABASE_URL: postgresql+asyncpg://auction_user:testpassword@localhost:5432/auction_test
          REDIS_URL: redis://localhost:6379/0
        run: cd bidding-engine && pytest integration/ -v --tb=short

  docker-build:
    name: Docker build check
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Build Postgres image
        run: docker build ./docker/postgres -t auction-postgres:ci
      - name: Compose up smoke test
        run: |
          cp .env.example .env
          docker compose up -d --wait
          docker compose ps
          docker compose down

  # Runs ON the target VM itself via a self-hosted runner — no SSH keys, no Tailscale
  # bridging from GitHub-hosted runners, no secrets store for connectivity. The staging
  # stack's real config lives in an env file OUTSIDE the git checkout (see
  # .env.staging.example's header) so `actions/checkout`'s working-tree clean can never
  # touch it. Adjust ENV_FILE below if you place it somewhere else.
  #
  # main is intentionally not deployed anywhere — CI still runs on it (see the `on:`
  # triggers above) so PRs into main are still gated, it just never reaches a server.

  deploy-staging:
    name: Deploy to staging
    runs-on: self-hosted
    needs: [lint-and-test, frontend-lint-and-build, integration-tests, docker-build]
    if: github.event_name == 'push' && github.ref == 'refs/heads/development'
    env:
      ENV_FILE: /opt/auctionhub/env/.env.staging
      COMPOSE_PROJECT: auctionhub-staging
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Build and start staging stack
        run: docker compose -p "$COMPOSE_PROJECT" --env-file "$ENV_FILE" up -d --build

      - name: Apply pending migrations (staging DB)
        run: |
          set -a; source "$ENV_FILE"; set +a
          DATABASE_URL="postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@localhost:${POSTGRES_PORT}/${POSTGRES_DB}" \
            python3 scripts/apply_migrations.py

      - name: Smoke test
        run: |
          set -a; source "$ENV_FILE"; set +a
          for i in $(seq 1 20); do
            if curl -fsS "http://localhost:${NGINX_PORT}/health" > /dev/null; then
              echo "staging up."
              exit 0
            fi
            sleep 10
          done
          echo "::error::staging never came up after deploy"
          exit 1

      - name: Prune old images
        run: docker image prune -f