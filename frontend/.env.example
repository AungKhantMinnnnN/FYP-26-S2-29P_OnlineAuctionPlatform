# Local Docker setup:
#   VITE_API_URL=/v1.0.0
# Local dev against shared VM (replace with VM's Tailscale IP):
#   VITE_API_URL=http://<TAILSCALE_IP>/v1.0.0
VITE_API_URL=https://backendurl:portnum/api/v1